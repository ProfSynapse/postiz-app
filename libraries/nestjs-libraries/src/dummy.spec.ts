// libraries/nestjs-libraries/src/dummy.spec.ts
// Sanity check that `npx nx test nestjs-libraries` discovers + executes
// specs in this library. Phase 0 scaffolding gate for the YouTube
// richer-metadata feature; remove only when Phase 2 provider specs exist.

describe('nestjs-libraries jest scaffolding', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
